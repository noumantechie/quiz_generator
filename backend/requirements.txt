# Core Flask dependencies
Flask==3.0.0
flask-cors==4.0.0
python-dotenv==1.0.0
Werkzeug>=3.0.0

# Groq API (NOT Google Generative AI)
groq==0.11.0
httpx>=0.27.0

# Lightweight sentence transformers (CPU-only)
sentence-transformers==2.3.1

# Scientific computing (without GPU dependencies)
scikit-learn==1.3.2
numpy==1.26.2
scipy

# Document processing
PyPDF2==3.0.1
python-docx==1.1.0

# Note: PyTorch will be installed by sentence-transformers
# but we'll use CPU-only version in Dockerfile
